# Login as user
mutation Login {
    login(credentials: { username: "cart", password: "cart" }) {
        token
    }
}

# Login as admin
mutation Login {
    login(credentials: { username: "admin", password: "admin" }) {
        token
    }
}