# Fetch all users
query GetAllUsers {
    users {
        id
        username
        roles {
            name
        }
    }
}

# Fetch user by ID
query GetUserById {
    user(id: 1) {
        id
        username
        roles {
            name
        }
    }
}